data:
  user:
    name: "Paul Becker"
    email: "REDACTED"
  editor: "nvim"
  visual: "emacsclient -c -a emacs"
  manpager: "less -R"

git:
  autoPush: true

merge:
  keep: "none"

apply:
  force: true
