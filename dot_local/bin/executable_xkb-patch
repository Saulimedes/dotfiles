#!/bin/sh
# Re-apply XKB keymap patch after session startup.
# Remaps: ; → dead_diaeresis (so ;a → ä, ;; → ;)
xkbcomp "$DISPLAY" - 2>/dev/null \
  | sed 's/\(\s*key <AC10>\s*{\s*\[\s*\)semicolon/\1dead_diaeresis/' \
  | xkbcomp -w 0 - "$DISPLAY" 2>/dev/null
