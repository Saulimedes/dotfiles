editor = "emacsclient -t -a ''"
visual = "emacsclient -c -a ''"

# name/email intentionally not here: they're prompted for at `chezmoi init`
# time and stored locally in ~/.config/chezmoi/chezmoi.toml (untracked), via
# .chezmoi.toml.tmpl. Keeps real contact info out of this public repo.
