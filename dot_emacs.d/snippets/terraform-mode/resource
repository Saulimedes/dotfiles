# -*- mode: snippet -*-
# name: resource block
# key: res
# --
resource "${1:aws_instance}" "${2:name}" {
  ${3:ami}           = "${4:value}"
  ${5:instance_type} = "${6:t3.micro}"

  tags = {
    Name = "${7:$2}"
  }
}
